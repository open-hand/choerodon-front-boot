#!/usr/bin/env node

const program = require('commander');
const Choerodon = require('../lib/index');

program
  .option('-c, --config <path>', 'set config path. defaults to ./bisheng.config.js')
  .option('-s, --skip <path>', 'skip install')
  .parse(process.argv);

Choerodon.build(program);
